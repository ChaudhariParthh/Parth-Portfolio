# Certificates Directory

This is the canonical directory for all certificate and achievement files.
Files placed here can be accessed directly in the browser via `/certificates/<filename>`.
Supported formats: `.pdf`, `.jpg`, `.jpeg`, `.png`, `.webp`.

